package tppe.deduplication;

import org.junit.platform.suite.api.SelectClasses;
import org.junit.platform.suite.api.Suite;

@Suite
@SelectClasses(Case3Test.class)
public class Case3Suite {
    // Classe vazia usada como ponto de entrada para a suite do Caso 3
}
